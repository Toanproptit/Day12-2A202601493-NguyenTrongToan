# Những file không cần thiết hoặc chứa dữ liệu cục bộ không được copy vào image.
# Bỏ sót .env = leak secret vào image. Bỏ sót .git = image phình to.
# Bổ sung đủ các mục còn thiếu — xem tests/test_cp2.py để biết yêu cầu.

.git
.gitignore
.env
.venv
venv
__pycache__
*.py[cod]
.pytest_cache
.mypy_cache
.coverage
tests
screenshots
*.md
docker-compose.yml
